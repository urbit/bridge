import React from 'react';
import { Button } from 'indigo-react';

// TODO: implement loading bar behind text
export default function LoadableButton({ ...rest }) {
  return <Button {...rest}></Button>;
}
