import React from 'react';
import { Button } from 'indigo-react';

// TODO: use svg icons
export const ForwardButton = props => <Button icon="→" {...props} />;
export const DownloadButton = props => <Button icon="⬇" {...props} />;
